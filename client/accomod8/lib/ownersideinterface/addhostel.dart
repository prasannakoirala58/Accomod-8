import 'package:flutter/material.dart';

class AddHostel extends StatelessWidget {
  const AddHostel({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SingleChildScrollView(
        child: Text("This is add hostel page"),
      ),
    );
  }
}
